block styles
  link(rel='stylesheet' href='/css/orderingQuestion.css')

if answerForm
  .form-group.col.s12
    ol#sortable
      - for (var index = 1; index <=4; index++)
        .row
          li.input-field.col.s10
            input.form-control(type='text', name='orderItem'+index, placeholder='Enter Answer Here', required)
          .input-field.col.s2
            a.btn-floating.btn-tiny.waves-effect.waves-light.red(onclick='$(this).parent().parent().remove()')
              i.tiny.material-icons close
  #addMoreAnswersButton
    a.waves-effect.waves-light.btn(onclick="addOrderingAnswer()") add answer

else if studentQuestionForm
  form.form-question#order_answerform
    ol#sortable
      each item in question.orderList
        .row
          li(name='option') #{item}
    button(class='btn btn-lg btn-primary btn-block' type='submit') Submit
else if adminQuestionEdit
  .form-group.col.s12
    ol#sortable
      each item, i in question.orderList
        .row
          li.input-field.col.s10
            input.form-control(type='text', name='orderItem'+index, placeholder='Enter Answer Here', required, value=item)
          .input-field.col.s2
            a.btn-floating.btn-tiny.waves-effect.waves-light.red(onclick='$(this).parent().parent().remove()')
              i.tiny.material-icons close
  #addMoreAnswersButton
    a.waves-effect.waves-light.btn(onclick="addOrderingAnswer()") add answer

block scripts
  script(src='/lib/jquery/jquery-sortable.js')
  script(src='/js/ordering-answer.js')
